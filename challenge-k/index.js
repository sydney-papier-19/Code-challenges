
const nickname= "Timmy";
const firstname = "Timothy";

//removed "", replaced it with template literals ``
console.log(`Good Morning, ${nickname} || ${firstname}!`)

if ( firstname === null && nickname === null ) {
    console.log("Good morning!");
}